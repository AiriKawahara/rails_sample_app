<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= render 'layouts/rails_default' %>
    <%= render 'layouts/shim' %>
  </head>
  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <%= yield %>
      <%= render 'layouts/footer' %>
      <!-- if Rails.env.development?は本番環境にデバッグ情報を表示しないためのコード -->
      <!-- if Rails.env.development?は開発環境でのみtrueになる -->
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
